<div class="formulario container col-md-8">
  <table class="table table-hover">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Name</th>
        <th scope="col">Description</th>
        <th scope="col">Address</th>
        <th scope="col">Text</th>
        <th scope="col">Edit</th>
        <th scope="col">Delete</th>
      </tr>
    </thead>

    <tbody>
      <% count = 1 %>
      <% @personalities.each do |personality| %>
        <tr>
          <th scope="row"><%= count %></th>
          <td> <%= personality.name %> </td>
          <td> <%= personality.address %> </td>
          <td> <%= personality.description%></td>
          <td><%= personality.text %></td>
          <td><%= link_to "Edit", edit_personality_path(personality), class: "btn btn-outline-secondary" %></td>
          <td><%= link_to "Delete", personality_path(personality), method: :delete, class: "btn btn-outline-danger" %></td>
        </tr>
        <% count += 1 %>

      <% end %>
    </tbody>
  </table>

</div>
